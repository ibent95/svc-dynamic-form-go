package middlewares

import (
	"github.com/labstack/echo/v4"
	"net/http"
)

func {{Name}}Middleware(next echo.HandlerFunc) echo.HandlerFunc {
	return func(ctx echo.Context) error {
		// TODO: implement {{name}} middleware logic
		return next(ctx)
	}
}
